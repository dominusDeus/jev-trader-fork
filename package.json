{
  "name": "jev-trader",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@ai-sdk/typesafe-ai": "^3.0.0",
    "@kuru-labs/kuru-sdk": "^0.0.95",
    "ai": "^7.0.103",
    "ethers": "5"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test --no-env-file --preload ./tests/setup.ts ./tests",
    "typecheck": "tsc --noEmit",
    "ibkr:inspect": "bun --no-env-file run src/ibkr/inspect.ts",
    "test:ibkr": "python3 -m unittest discover -s tests/ibkr -v"
  }
}
